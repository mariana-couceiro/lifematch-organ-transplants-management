info:
  name: candidate 1
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/lifematch/api/candidates/1
  body:
    type: json
    data: |-
      {
        "name": "Mariana",
        "birthDate": "2000-05-10",
        "bloodType": "A+",
        "requiredOrgan": "Kidney",
        "priority": 3,
        "waitingListEntryDate": "2026-09-11",
        "status": "WAITING"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
